# TODOS — 시스템 큐레이션 프로그램 (CEO 리뷰 2026-05-27)

표창키우기는 단일 `index.html`(7,250줄)에 **82개 Q-Leap 시스템**이 누적된 포화 게임.
개별 시스템은 견고하나 **중복·분산으로 인한 인지 부하**가 다음 병목.
큐레이션 = 가치(보상·효과)를 보존하면서 **표면을 통합**해 "잡일 더미 → 정돈된 깊이"로.

**핵심 제약 (모든 트랙 공통):**
- 상태 플래그(frenzyTimer, autoMergeEnabled 등)는 **세이브 호환 위해 보존**. UI만 재구성.
- UI 변경은 헤드리스 검증 불가 → **각 트랙 구현 후 브라우저 확인 게이트 필수**.
- 각 단계마다 `npm run check`(675 테스트) + `npm run scan`(중복 함수/id) 통과 유지.
- 단일 파일 보존. 로직 변경 시 회귀 테스트 추가.

권장 진행 순서: **T2 → T3 → T1(표시) → T4(감사) → T1·T4 제거 결정**
(로직-보존·고명확 먼저, 되돌리기 어려운 제거는 마지막 + 개별 승인.)

**구현 방식 (사용자 승인):** 단계적 + 검증 게이트 — 한 트랙 구현 → 브라우저 확인 → 다음.

**🔑 구현 발견 (2026-05-27 감사):** 기존 `auto-sell-box` 패널(index.html ~1690행)이
**자동화(자동매도·자동잠금·자동의식·윤회가속·우선순위)와 일일 항목(룰렛·보석환산소)을
한 컬럼에 혼재**시킴. auto-merge 버튼은 spawn 바(~1517)에 분리, 명상·폭주는 헤더(~1486).
→ T2·T3는 이 혼재 패널을 **분리**하는 작업으로 얽힘:
  - T2 = 자동화 항목(자동매도/잠금/의식/윤회가속/우선순위 + auto-merge + 명상)을 한 패널로.
  - T3 = 일일 항목(룰렛/환산소 + 미션/도전/합성/출석)을 별도 '오늘' 허브로.
  - 두 트랙을 **연속 구현**(같은 패널을 가르므로)하되 각각 검증 게이트.
핸들러 id 보존 필수(auto-sell-toggle, spin-btn 등) — 재배선 시 동작 유지.

---

## T2 — 자동화 UI 단일화  [우선순위 P1 · 위험 낮음 · 로직 불변]

**문제:** 자동 토글 6개(autoMerge/autoSell/autoRitual/autoLock/autoFrenzy/prestigeSpeed)
+ 명상(전체 ON)이 메인 화면에 스위치 벽으로 노출.

**계획:**
- 단일 "⚙ 자동화" 모달/패널에 모든 토글 + 설정(autoMergeCap, autoSellThreshold,
  autoLockThreshold, autoMergePriority) 수납.
- 메인 화면엔 진입점 1개 + 컴팩트 상태 요약(예: "자동 3/6 ON").
- 명상 = 패널 내 "전체 ON" 버튼으로 재배치.
- 상태 플래그·동작 전부 불변. DOM 이동 + 이벤트 핸들러 재배선만.

**위험/검증:** 이벤트 핸들러 재배선 누락 시 토글 무반응 → 브라우저에서 각 토글 동작 확인.
**스코프:** index.html (HTML 패널 + refreshUI 동기화 + 핸들러). 테스트: 기존 로직 테스트 유지.

---

## T3 — 일일 허브 통합  [우선순위 P1 · 위험 낮음 · 보상 불변]

**문제:** 일일미션·주간미션·일일도전·일일합성·룰렛·출석 6개가 산재(quest-btn, spin-btn,
배너, 출석 모달). 신규 플레이어가 "오늘 할 것"을 한눈에 못 봄.

**계획:**
- 단일 "📅 오늘" 허브 모달: 일일미션(3) + 주간미션 + 일일도전 진행 + 일일합성 티어 +
  룰렛 + 출석 상태 + claim 버튼을 한 패널에 집약.
- 기존 claim 함수(claimQuestRewards/claimWeeklyReward/doSpin/...) 재사용 — 보상 로직 불변.
- 산재된 배너는 요약 또는 허브로 위임.

**위험/검증:** claim 버튼 재배선 + 진행도 readout 정확성 → 브라우저에서 각 claim 확인.
**스코프:** index.html (허브 모달 + 렌더 함수 통합). 보상 경로는 이미 테스트됨.

---

## T1 — 타이머 버프 통합  [우선순위 P2 · 표시=저위험, 제거=T4와 병합]

**문제:** 폭주·골드러시·버닝·시간가속 4개 timed-buff. "지금 무슨 버프?" 혼란.
효과 중복: 골드러시(골드×2)⊂폭주(골드+20%), 버닝(자동합치기+가속)⊂폭주+명상,
시간가속(생성가속)⊂폭주(생성×2).

**계획 (2부):**
- **T1a (저위험):** 활성 버프 통합 HUD 표시 — 현재 켜진 모든 timed-buff를 한 곳에
  남은 시간과 함께 표시. 혼란 즉시 해소. 효과·트리거 전부 불변.
- **T1b (T4와 함께 결정):** 버닝/시간가속을 폭주로 흡수하거나 제거할지 — 젬 sink·밸런스
  영향이라 사용자 플레이테스트 판단 필요. 개별 승인.

**위험/검증:** T1a는 표시만. T1b는 밸런스 변경 → 사용자 결정 + 검증.

---

## T4 — 저사용 시스템 감사  [우선순위 P2 · 제거는 개별 승인]

**문제:** 니치 시스템이 인지 부하에 기여. 엔진 측 사용량 측정 불가(애널리틱스 없음) →
디자인 판단 + 사용자 결정.

**제거/통합 후보 (각각 사용자 승인 필요):**
- `burning`(💎10 자동합치기 창) — 명상 자동합치기와 중복 후보.
- `timeBoost`(shop 생성가속) — 폭주 생성×2와 중복 후보.
- `split`(분할 shop) — 니치. 유지/제거 판단.
- `goldenCoat`(황금 코팅 shop) — 니치. 변종 융합과 역할 겹침 여부 검토.

**방법:** 후보별 What/Why/대체재/위험을 정리해 개별 AskUserQuestion → 승인된 것만 제거.
제거 시 세이브 마이그레이션(해당 state/stat 무해화) + 테스트 갱신 + scan clean.

**위험:** 사용자가 아끼는 걸 제거할 수 있음 → 반드시 개별 승인. 되돌리기 어려운 문(one-way).

---

## 실패 모드 레지스트리 (큐레이션 리팩토링)

| 코드패스 | 실패 모드 | 방지책 | 검증 |
|---|---|---|---|
| UI 패널 재구성 | 이벤트 핸들러 미배선 → 버튼 무반응 | 핸들러를 패널 렌더 후 재바인드 | 브라우저 클릭 |
| 상태 플래그 이동 | 세이브 비호환 → 토글 상태 손실 | 플래그명·load 경로 보존 | validateAndRepairState 테스트 |
| 시스템 제거(T4) | 잔존 참조 → ReferenceError | scan(중복/dead) + grep 잔존 참조 | npm run check |
| 배너/모달 통합 | getElementById 중복 id | scan 중복-id 체크(v3.66.7) | npm run scan |
